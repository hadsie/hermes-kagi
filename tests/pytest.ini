[pytest]
# Config lives in tests/ (not the plugin root) on purpose: it pins pytest's
# rootdir here so the plugin's own package __init__.py one level up is never
# collected as a test package. Run with `pytest tests/` or from inside tests/.
addopts = --import-mode=importlib
